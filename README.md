# CNN
The goal of this task is:

Build a Convolutional Neural Network (CNN) from scratch to detect emotion in facial images (Problem 1 and 2),
Use data augmentation to increase the size of training data (Problem 3),
Use transfer learning to customize a pretrained model to solve the same problem (Problem 4).
